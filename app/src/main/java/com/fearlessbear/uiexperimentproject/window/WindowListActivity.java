package com.fearlessbear.uiexperimentproject.window;

import com.fearlessbear.uiexperimentproject._base.BaseActivity;

public class WindowListActivity extends BaseActivity {
    {
        mTitle = new String[]{
                "Dialog window 与 Activity window 的关系",
        };
        mClasses = new Class[]{
                DialogWindowRelationship.class,
        };
    }
}
